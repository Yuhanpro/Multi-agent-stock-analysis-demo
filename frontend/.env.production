# Production build: empty API base => same-origin relative /api/... requests.
# nginx on the public origin proxies /api to the FastAPI backend, so this works
# unchanged behind http://47.93.21.132:18080 and any future https domain.
NEXT_PUBLIC_API_BASE=
